name: Deploy Sitemap
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: touch .nojekyll
      - uses: actions/upload-pages-artifact@v2